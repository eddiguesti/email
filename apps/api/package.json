{
  "name": "@lb-bot/api",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "build:tsc": "tsc",
    "dev": "func start --typescript",
    "start": "func start",
    "watch": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@azure/functions": "^4.5.0",
    "@lb-bot/shared": "workspace:*",
    "@microsoft/microsoft-graph-client": "^3.0.7",
    "@supabase/supabase-js": "^2.39.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "azure-functions-core-tools": "^4.0.0",
    "typescript": "^5.3.0"
  }
}
